import 'package:flutter/material.dart';

class Haditpage extends StatelessWidget {
  const Haditpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:
      Center(
        child: Text('Hadit'),
      ),
    ) ;
  }
}
